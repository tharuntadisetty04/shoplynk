name: Ping Render Server

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send Ping to Vercel API
        run: curl -X GET https://your-vercel-app.vercel.app/api/pingServer
